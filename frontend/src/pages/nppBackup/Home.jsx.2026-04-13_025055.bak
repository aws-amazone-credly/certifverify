import { useState } from "react";
import { useNavigate } from "react-router-dom";
import Header from "../components/Header";

export default function Home() {
  const [code, setCode] = useState("");
  const navigate = useNavigate();

  function handleVerify(e) {
    e.preventDefault();
    const trimmed = code.trim();
    if (trimmed) navigate(`/verify/${trimmed}`);
  }

  return (
    <div className="min-h-screen flex flex-col">
      <Header />

      {/* Hero */}
      <main className="flex-1 flex flex-col items-center justify-center px-4 text-center">
        <div className="max-w-2xl w-full space-y-6">
          <div className="space-y-3">
            <span className="text-6xl">🔐</span>
            <h1 className="font-display font-bold text-4xl sm:text-5xl text-slate-900 leading-tight">
              Vérifiez l'authenticité<br />d'une certification IT
            </h1>
            <p className="text-slate-500 text-lg">
              Entrez le code de vérification figurant sur le certificat
              pour confirmer son authenticité en quelques secondes.
            </p>
          </div>

          {/* Search bar */}
          <form onSubmit={handleVerify} className="flex gap-2 max-w-lg mx-auto">
            <input
              type="text"
              value={code}
              onChange={(e) => setCode(e.target.value)}
              placeholder="Ex: V3rK2mXpQ8nL"
              className="flex-1 px-4 py-3 rounded-xl border border-slate-300 focus:outline-none
                         focus:ring-2 focus:ring-brand-400 font-mono text-sm bg-white"
            />
            <button
              type="submit"
              disabled={!code.trim()}
              className="px-6 py-3 rounded-xl bg-brand-500 text-white font-semibold
                         hover:bg-brand-600 disabled:opacity-40 disabled:cursor-not-allowed transition-colors"
            >
              Vérifier
            </button>
          </form>

          {/* Features */}
          <div className="grid grid-cols-1 sm:grid-cols-3 gap-4 pt-8 text-sm">
            {[
              { icon: "⚡", label: "Instantané", desc: "Résultat en moins d'une seconde" },
              { icon: "🔒", label: "Fiable",     desc: "Données stockées sur Firebase" },
              { icon: "📱", label: "QR Code",    desc: "Scan direct depuis le badge" },
            ].map(({ icon, label, desc }) => (
              <div key={label} className="bg-white rounded-xl border border-slate-200 p-4 text-center">
                <span className="text-2xl">{icon}</span>
                <p className="font-semibold text-slate-800 mt-1">{label}</p>
                <p className="text-slate-500 text-xs mt-1">{desc}</p>
              </div>
            ))}
          </div>
        </div>
      </main>

      <footer className="text-center text-xs text-slate-400 py-6">
        © {new Date().getFullYear()} CertVerify — Plateforme de vérification de certifications IT
      </footer>
    </div>
  );
}
